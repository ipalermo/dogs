<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dogbuddy.android.code.test.dogsapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.dogbuddy.android.code.test.dogsapp.dogs.DogsActivity"
            android:theme="@style/AppTheme.OverlapSystemBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dogbuddy.android.code.test.dogsapp.dogdetail.DogDetailActivity" />
        <activity android:name="com.dogbuddy.android.code.test.dogsapp.addeditdog.AddEditDogActivity" />
        <activity
            android:name="com.dogbuddy.android.code.test.dogsapp.statistics.CreditsActivity"
            android:parentActivityName="com.dogbuddy.android.code.test.dogsapp.dogs.DogsActivity"
            tools:ignore="UnusedAttribute">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dogbuddy.android.code.test.dogsapp.dogs.DogsActivity" />
        </activity>
    </application>

</manifest>
